openapi: 3.0.3
info:
  title: Contacts API
  version: 1.0.0
# DELIBERATELY BAD SPEC — do not pre-fix. Missing: securitySchemes/security,
# rate-limit annotations, request-body schema properties, response content/schemas,
# operationIds, summaries, and 4xx/5xx error responses. Drives a low Catalog scorecard.
paths:
  /contacts:
    get:
      responses:
        '200':
          description: ok
    post:
      requestBody:
        content:
          application/json:
            schema:
              type: object
      responses:
        '201':
          description: created
  /contacts/{id}:
    get:
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: ok
